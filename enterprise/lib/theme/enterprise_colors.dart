import 'package:flutter/material.dart';


const Color kGradientStart = Color(0xFFEF4135); // strong red (header & FAB)
const Color kGradientEnd   = Color(0xFFE53935); // slightly darker red tone

/// Accessible UI tones
const Color kPrimaryDark   = Color(0xFFB71C1C); // deeper red for contrast
const Color kPrimary       = Color(0xFFEF4135); // main red tone
const Color kAccent        = Color.fromARGB(255, 241, 131, 124); // vibrant red accent
const Color kOnPrimary     = Colors.white;      // text/icons on red background

///  Surfaces & text
const Color kSurface       = Color(0xFFF5F6F8); // light gray background
const Color kOnSurface     = Color(0xFF1E1E1E); // dark text color
const Color kOutline       = Color(0xFFE0E0E0); // soft outline color for cards

///  Error tones
const Color kError         = Color(0xFFD32F2F);
const Color kOnError       = Colors.white;
